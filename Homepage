<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles.css" />
    <title>SendIT</title>
  </head>
<style>* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Set a background gradient for the body with parallax effect */
body {
  background: linear-gradient(to bottom, #f6fafd, #b3e0f4);
  font-family: Arial, sans-serif;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  margin: 0;
  background-attachment: fixed;
  background-size: cover;
  background-position: center;
}

/* Create a container for your content with a shadow and animation */
.container {
  background-color: #ffffff;
  border-radius: 8px;
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
  padding: 20px;
  text-align: center;
  max-width: 400px;
  width: 100%;
  animation: fadeIn 1s ease-in-out;
}

/* Style headings with a subtle shadow and animation */
h1 {
  font-size: 28px;
  margin-bottom: 20px;
  text-shadow: 2px 2px 2px rgba(0, 0, 0, 0.2);
  animation: fadeInUp 1s ease-in-out;
}

/* Style form elements with a gradient border and transitions */
form {
  display: flex;
  flex-direction: column;
  align-items: center;
}

label {
  font-weight: bold;
  margin-bottom: 10px;
}

input[type="text"],
input[type="email"],
input[type="password"] {
  width: 100%;
  padding: 12px;
  margin: 8px 0;
  border: 1px solid #ccc;
  border-radius: 20px;
  background: linear-gradient(to bottom, #ffffff, #f2f2f2);
  transition: border-color 0.3s, background-color 0.3s;
}

input[type="text"]:focus,
input[type="email"]:focus,
input[type="password"]:focus {
  border-color: #007bff;
  background-color: #ffffff;
}

button[type="submit"] {
  background-color: #007bff;
  color: #fff;
  padding: 12px 24px;
  border: none;
  border-radius: 20px;
  cursor: pointer;
  font-size: 16px;
  margin-top: 20px;
  transition: background-color 0.3s, transform 0.2s;
}

button[type="submit"]:hover {
  background-color: #0056b3;
  transform: scale(1.05);
}

/* Add a link style with a subtle gradient and hover animation */
#signup-link a {
  text-decoration: none;
  color: #007bff;
  background: linear-gradient(to bottom, transparent 50%, #b3e0f4 50%);
  background-size: 100% 200%;
  background-position: 0 0;
  transition: background-position 0.3s, color 0.3s;
}

#signup-link a:hover {
  background-position: 0 -100%;
  color: #0056b3;
  text-decoration: underline;
}

/* Style buttons with creative designs and hover animation */
.buttons {
  display: flex;
  justify-content: space-between;
  margin-top: 30px;
}

.buttons button {
  flex-grow: 1;
  margin: 0 10px;
  padding: 10px 20px;
  background-color: #ff6b6b;
  color: #fff;
  border: none;
  border-radius: 20px;
  cursor: pointer;
  font-size: 16px;
  transition: background-color 0.3s, transform 0.2s;
}

.buttons button:hover {
  background-color: #ff3b3b;
  transform: scale(1.05);
  animation: bounce 0.5s ease-in-out;
}

/* Additional styles for specific elements can be added here */

/* Keyframe animations */
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes fadeInUp {
  from {
    transform: translateY(20px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

@keyframes bounce {
  0%,
  20%,
  50%,
  80%,
  100% {
    transform: translateY(0);
  }
  40% {
    transform: translateY(-20px);
  }
  60% {
    transform: translateY(-10px);
  }
}

/* Responsive design for smaller screens */
@media (max-width: 768px) {
  .container {
    max-width: 90%;
  }

  h1 {
    font-size: 24px;
  }

  input[type="text"],
  input[type="email"],
  input[type="password"] {
    font-size: 14px;
  }

  button[type="submit"] {
    font-size: 14px;
  }
}
</style>
  <body>
    <div>
      <h1>Welcome to SendIT</h1>
    </div>
    <div class="container">
      <div class="buttons">
        <button id="signup-button">Sign Up</button>
        <button id="signin-button">Sign In</button>
      </div>
    </div>
    <script>
  document.getElementById("signup-button").addEventListener("click", () => {
  window.location.href = "homepage.html";
});

document.getElementById("signin-button").addEventListener("click", () => {
  window.location.href = "homepage.html";
});</script>
  </body>
</html>
